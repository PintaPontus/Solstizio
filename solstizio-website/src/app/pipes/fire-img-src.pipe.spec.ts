import {FireImgSrcPipe} from './fire-img-src.pipe';

describe('FireImgSrcPipe', () => {
    it('create an instance', () => {
        const pipe = new FireImgSrcPipe();
        expect(pipe).toBeTruthy();
    });
});
